const budgetRegRequest = {
  budget: "/budgets", // + {member_id} 예산생성
  budgetList: "/budgets", //get 예산 조호ㅢ
  modifyBudget: "/budgets/detail", // + {budget_id} 예산 수정
};

export default budgetRegRequest;
